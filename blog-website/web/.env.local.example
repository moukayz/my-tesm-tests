# Local dev overrides (higher precedence than .env.app).
# Next.js rewrites /v1/* -> ${API_ORIGIN}/v1/*
# This file may contain localhost/127.0.0.1 values.
API_ORIGIN=http://127.0.0.1:3000

# Optional: override WEB_PORT if you need a different dev port.
# WEB_PORT=3001
