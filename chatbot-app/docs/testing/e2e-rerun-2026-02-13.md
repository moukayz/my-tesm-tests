# E2E Rerun Report - 2026-02-13

## Scope
- Rerun Playwright E2E suite with no-login/local-only requirements.

## Command
```bash
./scripts/e2e-run.sh
```

## Result
- Status: failed (web server startup)

## Failure Details
- Playwright webServer failed to start Next dev due to existing lock.
- Error: `Unable to acquire lock at apps/web/.next/dev/lock, is another instance of next dev running?`

## Notes
- E2E config auto-selected ports, resulting in `E2E_API_URL` and `E2E_BASE_URL` both set to `http://localhost:3003`.
- The failure occurred before tests executed.

## Minimal Repro
1. Ensure another `next dev` instance is running for `apps/web`.
2. Run `./scripts/e2e-run.sh` from repo root.
3. Observe Playwright webServer lock error.
